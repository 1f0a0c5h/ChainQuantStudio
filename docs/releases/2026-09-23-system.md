# System release review — 2026-09-23

## Scope and evidence

- Public base: `dde718c`; system code commit: `8d2f585`.
- Gateway indexes both legacy top-level reports and versioned nested backtest manifests. It prefers an executed performance report over an earlier dataset-gate record for the same work order, and serves only allowlisted artifacts below `reports/`.
- Data Service materializes the exact market and timeframe declared by a versioned strategy artifact, verifies continuous closed candles, and records content and provenance hashes. No strategy implementation, signal definition, spec, historical dataset, runtime state, or backtest result is part of this release.
- User-spec parsing, provenance verification, and desktop handoff animation received generic fixes. Trading remains disabled; no order, withdrawal, or transfer API was added.
- This is a framework update only. No stable strategy or signal ID/version is promoted, so Artifact Registry and live-approval matching are not applicable to this release.

## Verification

Run from the public worktree with the existing Python environment and `PYTHONPATH=src`:

- Secret scan: passed. `.env` remains ignored.
- Ruff: passed. Mypy: passed for 91 source files.
- Pytest with branch coverage: **307 passed, 11 skipped; 74.79% coverage** (required 73%). The skipped tests require opt-in calls to public exchange endpoints.
- Python compileall and `git diff --check`: passed.
- Dashboard ESLint, Vinext production build, and handoff-animation test: passed. The local public worktree shares `node_modules` with the private worktree; the equivalent installed CLI entry points were invoked directly because pnpm correctly refused to recreate an external junction.
- Focused Gateway tests cover legacy and nested report artifacts, blocked-versus-complete selection, and artifact serving. Safety tests cover the absence of exchange-action endpoints and explicit approval gates.

## Operational limits and rollback

- Historical replay results remain research-only; this release does not approve or enable a live signal or trading strategy.
- Live exchange integration tests were skipped, so no claim is made about current exchange availability.
- A running Gateway must restart to load the new report index. The loopback `/health` endpoint and report artifact URLs were verified locally before publication.
- If necessary, revert the system code commit from the public branch to restore the previous framework. Keep local runtime data and private strategy artifacts untouched.
