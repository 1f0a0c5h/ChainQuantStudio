{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-default",
  "description": "Default capability for the local CHA!N desktop control window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
